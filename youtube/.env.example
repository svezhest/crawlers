# Everything is optional. Comma-separated list of local SOCKS5 proxy ports to
# rotate across when YouTube IP-blocks the anonymous transcript endpoint.
YT_PROXY_PORTS=
# Base delay (s) between successful fetches when going direct, plus jitter (s).
YT_DELAY=20
YT_JITTER=10
